// program to convert from uppercase to lowercase


#include<stdio.h>
#include<string.h>
int main(){
printf("enter the name");
char a[20];
scanf("%s",a);
printf("\n");

for(int i=0; a[i]!='\0';i++){
    if(a[i]>='A'  && a[i]<='Z'){
        a[i]=a[i]+32;
    }
}printf(a);
}
