// program to find the length of the string

#include<stdio.h>
#include<string.h>
int main(){
printf("enter the name");
char name[30];
scanf("%s",name);
int count=strlen(name);      // strlen function is the predefined function to find the length of the string
printf("the length of string is %d",count);

}
