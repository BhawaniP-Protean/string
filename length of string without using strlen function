#include<stdio.h>
#include<string.h>
int main(){
printf("enter the name");
char name[30];
scanf("%s",name);
int count=0;
int i=0;
while(name[i]!='\0'){
    count=count+1;
    i++;
}
printf("the length of string is %d",count);

}
